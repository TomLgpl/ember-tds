{{page-title "Step1"}}
<MultiSelect @elements={{this.dispoItems}} @selected={{this.dispoItems_}} @onChange={{this.changeDispo}}/>
<button {{action "add" this.dispoItems this.includedItems this.dispoItems_}}>
  Ajouter
</button>
<button {{action "add" this.includedItems this.dispoItems this.includedItems_}}>
  Retirer
</button>
<MultiSelect @elements={{this.includedItems}} @selected={{this.includedItems_}} @onChange={{this.changeIncluded}}/>


