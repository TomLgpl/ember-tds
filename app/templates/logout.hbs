{{page-title "Logout"}}
<div class="ui container">
  <div class="ui segment">
    <h2>Vous avez bien été déconnecté</h2>
    <button {{ action "toIndex" }} class="btn btn-primary">Retour à la page de connexion</button>
  </div>
</div>

{{outlet}}